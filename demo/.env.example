# ===================================================================
# EXEMPLO DE ARQUIVO .ENV PARA PRODUÇÃO
# ===================================================================
# COPIE este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env no Git! Adicione ao .gitignore
# ===================================================================

# ===================================================================
# BANCO DE DADOS
# ===================================================================
DB_URL=jdbc:mariadb://localhost:3306/seu_banco
DB_USERNAME=seu_usuario
DB_PASSWORD=sua_senha_super_secreta

# ===================================================================
# JWT AUTHENTICATION
# ===================================================================
# Gerar chave segura com: openssl rand -base64 32
# IMPORTANTE: Altere esta chave em produção!
JWT_SECRET=sua_chave_jwt_super_secreta_aqui

# Tempo de expiração do token JWT em milissegundos
# 86400000 ms = 24 horas
JWT_EXPIRATION=86400000

# Tempo de expiração do refresh token em milissegundos
# 604800000 ms = 7 dias
JWT_REFRESH_EXPIRATION=604800000

# ===================================================================
# PAGAMENTO PIX
# ===================================================================
# Chave PIX da sua loja (CPF, CNPJ, e-mail, telefone ou chave aleatória)
PIX_CHAVE=12345678000190

# Nome do beneficiário (nome da loja)
PIX_NOME_BENEFICIARIO=Sua Loja LTDA

# Cidade da loja
PIX_CIDADE=Sao Paulo

# Tempo de expiração do PIX em minutos
PIX_EXPIRACAO_MINUTOS=15

# ===================================================================
# CRIPTOGRAFIA (para implementar)
# ===================================================================
# Chave de criptografia AES-256 para dados sensíveis (cartões)
# Gerar com: openssl rand -base64 32
ENCRYPTION_KEY=sua_chave_de_criptografia_aes256

# ===================================================================
# SERVIDOR (PRODUÇÃO)
# ===================================================================
# Porta da aplicação
SERVER_PORT=8080

# Perfil Spring (dev, staging, prod)
SPRING_PROFILES_ACTIVE=prod

# ===================================================================
# SSL/HTTPS (PRODUÇÃO)
# ===================================================================
# Habilitar HTTPS
SERVER_SSL_ENABLED=true

# Caminho para o arquivo do certificado SSL (keystore)
SERVER_SSL_KEY_STORE=/caminho/para/keystore.p12

# Senha do keystore
SERVER_SSL_KEY_STORE_PASSWORD=senha_do_keystore

# Tipo do keystore (PKCS12 ou JKS)
SERVER_SSL_KEY_STORE_TYPE=PKCS12

# Alias do certificado
SERVER_SSL_KEY_ALIAS=tomcat
