# Propriedades de teste (não usar H2)
# Propriedades para o JwtService usado nos testes
jwt.secret=Zm9vYmFyYmF6MTIzNDU2
jwt.expiration=3600000
jwt.refresh-token.expiration=86400000

# Credenciais mínimas para testes que dependem de Spring Security (não seguro, só para testes)
spring.security.user.name=test
spring.security.user.password=test

# ===================================================================
# CONFIGURAÇÃO DO BANCO DE DADOS (DATASOURCE)
# ===================================================================
spring.datasource.url=jdbc:mariadb://localhost:3306/backend
spring.datasource.username=root
spring.datasource.password=aluno
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=update
